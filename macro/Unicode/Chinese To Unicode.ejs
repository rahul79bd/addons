/**
 * ptma(ptma@163.com)
 */
Include(App.AppPath + "\\macro\\Unicode\\unicode.js");
var pos = App.ActiveDoc.CaretPos;
if (App.ActiveDoc.HasSel()) {
    App.ActiveDoc.SelText = Unicode.toUnicode(App.ActiveDoc.SelText);
} else {
    App.ActiveDoc.Text = Unicode.toUnicode(App.ActiveDoc.Text);
}
App.ActiveDoc.SetCaretPos(pos.Line, pos.Col, true);